name: CD

on:
  workflow_dispatch:
  push:
    branches: [main, master]
    tags: ['v*']

env:
  REGISTRY: ghcr.io
  BACKEND_IMAGE: ghcr.io/${{ github.repository }}/backend
  FRONTEND_IMAGE: ghcr.io/${{ github.repository }}/frontend

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push backend
        uses: docker/build-push-action@v6
        with:
          context: ./src
          file: ./src/webapi/cnt_sp_webapi/6 WebApp/Dockerfile
          push: ${{ github.event_name != 'workflow_dispatch' || github.ref_type == 'tag' }}
          tags: |
            ${{ env.BACKEND_IMAGE }}:latest
            ${{ env.BACKEND_IMAGE }}:${{ github.sha }}

      - name: Build and push frontend
        uses: docker/build-push-action@v6
        with:
          context: ./src/frontend/cnt_sp_web
          file: ./src/frontend/cnt_sp_web/Dockerfile
          push: ${{ github.event_name != 'workflow_dispatch' || github.ref_type == 'tag' }}
          tags: |
            ${{ env.FRONTEND_IMAGE }}:latest
            ${{ env.FRONTEND_IMAGE }}:${{ github.sha }}

  # Шаблон deploy — раскомментируйте и настройте под свой кластер
  # deploy-staging:
  #   needs: build-and-push
  #   runs-on: ubuntu-latest
  #   environment: staging
  #   steps:
  #     - run: echo "Deploy to staging TBD"
