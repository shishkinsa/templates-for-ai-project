import type { ThemeConfig } from 'antd';

/** Токены Ant Design для `ConfigProvider`. */
const theme: ThemeConfig = {
  token: {
    colorPrimary: '#1890ff',
    borderRadius: 6,
    fontSize: 14,
  },
};

export default theme;
